[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "voxpilot"
version = "0.1.0"
description = "Real-time voice AI pipeline"
dependencies = []

[tool.setuptools.packages.find]
where = ["."]
include = ["server*"]
